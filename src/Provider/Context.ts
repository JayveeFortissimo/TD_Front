import { createContext } from 'react';

const Creation1 = createContext<any>(null);
const Creation2 = createContext<any>(null);
const Creation3 = createContext<any>(null);
const Creation4 = createContext<any>(null);

export{
    Creation1,
    Creation2,
    Creation3,
    Creation4
} ;



